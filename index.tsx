import React from 'react';
import ReactDOM from 'react-dom/client';
import ProteinTracker from './main';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ProteinTracker />
  </React.StrictMode>
);
